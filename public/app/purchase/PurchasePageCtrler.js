angular.module('mainApp')
.controller('PurchasePageCtrler', 
  ['$http', '$window', '$state', 
  function($http, $window, $state) {
    //TODO:
    
    //1. create purchase price and period
    //2. do purchase(account deposit)
    
  }]
);